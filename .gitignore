.venv/
__pycache__/
*.pyc
*.so
.pytest_cache/
.ruff_cache/
.DS_Store
build/
dist/
*.egg-info/
*.ncu-rep

# Build products of the cross language checks. The sources in verify/ are
# committed; nothing they compile to is.
verify/pendulum/target/

# results/ is deliberately NOT ignored -- benchmark CSVs are committed so the
# README tables stay reproducible.
